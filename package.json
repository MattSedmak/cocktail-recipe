{
  "name": "cocktail-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "web-dev-server --open / --node-resolve",
    "dev": "web-dev-server --open / --node-resolve --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "haunted": "^5.0.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "@web/dev-server-esbuild": "^1.0.2",
    "typescript": "^5.6.3"
  }
}
